# Placeholder API for the collection showcase.
# Replace this URL with the real backend endpoint when it appears.
NEXT_PUBLIC_COLLECTIONS_API_URL=/api/catalog

# Placeholder API for product/collection lead forms.
# Replace this URL with the real CRM/backend endpoint when it appears.
NEXT_PUBLIC_LEAD_API_URL=/api/leads

# Legacy aliases are still supported by app/lib/catalogApi.ts:
# NEXT_PUBLIC_CATALOG_API_URL=/api/catalog
# NEXT_PUBLIC_ORDER_API_URL=/api/leads
